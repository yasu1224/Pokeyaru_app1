= form_with(model: rare, local: true) do |form|
  - if rare.errors.any?
    #error_explanation
      %h2
        = pluralize(rare.errors.count, "error")
        prohibited this rare from being saved:
      %ul
        - rare.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :rarity
    = form.text_field :rarity
  .actions
    = form.submit
